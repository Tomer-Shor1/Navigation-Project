# ---- flight data (far too large for git; supplied by the course) -----------
data/raw/*.mp4
data/raw/*.srt
data/frames/

# ---- downloaded satellite basemaps -----------------------------------------
# Large, and the imagery belongs to whichever provider it came from. Regenerate
# with tools/fetch_basemap.py.
data/basemap/

# ---- results not yet regenerated under the corrected protocol --------------
# See "Known limitations" in RESULTS.md. Run ./reproduce.sh to produce them.
results/flight_0017/
results/flight_0023/
results/_ablation/

# ---- long-form draft notes, superseded by README.md + RESULTS.md -----------
# Kept locally, not published. Delete this line to include it.
EXPLANATION.txt

# ---- python ----------------------------------------------------------------
__pycache__/
*.pyc
*.pyo
venv/
.venv/
*.egg-info/

# ---- editors / OS ----------------------------------------------------------
.DS_Store
.idea/
.vscode/
.claude/
